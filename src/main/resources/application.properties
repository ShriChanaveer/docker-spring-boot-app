# Application Name
spring.application.name=spring-boot-app

# Server Configuration
server.port=${SERVER_PORT:8080}

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics,env
management.endpoint.health.show-details=when-authorized
management.health.defaults.enabled=true

# Logging Configuration
logging.level.root=${LOG_LEVEL:INFO}
logging.level.com.example.app=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %logger{36} - %msg%n

# Database Configuration (uncomment if using database)
# spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/appdb}
# spring.datasource.username=${DATABASE_USERNAME:appuser}
# spring.datasource.password=${DATABASE_PASSWORD:apppassword}
# spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration (uncomment if using JPA)
# spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
# spring.jpa.show-sql=${JPA_SHOW_SQL:false}
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.properties.hibernate.format_sql=true

# Connection Pool Configuration (uncomment if using database)
# spring.datasource.hikari.maximum-pool-size=10
# spring.datasource.hikari.minimum-idle=5
# spring.datasource.hikari.connection-timeout=30000
# spring.datasource.hikari.idle-timeout=600000
# spring.datasource.hikari.max-lifetime=1800000
